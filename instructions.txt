_ After setting debugger, run the following command on the same terminal where we have to run the node server:
        export DEBUG=development:*
_ Now start the server
_ Jb tk debugger chalta rahy ga, value show hoti rahy gi.

_ To stop the value printed for debugger, we can run the command:
        export DEBUG=