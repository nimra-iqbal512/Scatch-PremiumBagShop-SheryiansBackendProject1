console.log(process.env.NODE_ENV);  // To check the value of set environment. If 'undefined', then value is set.

== set value for environment variables using below command.
      export NODE_ENV=development

Now test the route /owners/create in postman